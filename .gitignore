# Local editor / agent state
.claude/

# OS junk
.DS_Store
Thumbs.db

# Anything secret-shaped
*.env
.env*

# Wrangler local state (Workers Static Assets watcher writes here at repo root
# because worker/wrangler.toml uses `[assets] directory = "../"`)
.wrangler/
